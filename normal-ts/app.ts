// primitive
let myAge: number;
myAge = 27;

// array
let hobbies: string[] = ["Cooking", "Shopping"];

// enum
enum Color {
    Red,
    Blue = 100,
    Green
}
let color = Color.Green;
console.log(color);
